/* Задача:
Обратный массив, вернуть результат. Не используйте Array.prototype.reverse. У вас есть 30 байт, чтобы сэкономить.

Пример: [1, 2, 3] → [3, 2, 1]
*/

const reverse = (a) => a.map(a.pop, [...a]);

console.log(reverse([1, 2, 3])); // expected [3, 2, 1]
console.log(reverse([..."01234567890123456789"])); // expected [..."98765432109876543210"]
